export const metadata = {
  title: "Solobank AI",
  description: "AI-powered DeFi assistant on Solana",
};

export default function AiLayout({ children }: { children: React.ReactNode }) {
  return children;
}
